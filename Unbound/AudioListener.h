#pragma once
#include"stdafx.h"

namespace Unbound
{
	namespace Audio
	{
		/*A class to represent the audio listener in a scene and manage its properties.*/
		class AudioListener
		{
		public:

			AudioListener() noexcept;
			~AudioListener();
		private:
		};
	}
}